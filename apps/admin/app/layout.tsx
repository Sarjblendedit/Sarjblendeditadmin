import './styles.css';
export const metadata = { title: 'SARJ BLENDED IT — Admin', description: 'Mobile barber operations dashboard' };
export default function Layout({ children }: { children: React.ReactNode }) { return <html lang="en"><body>{children}</body></html>; }

