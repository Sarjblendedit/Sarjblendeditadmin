{
  "name": "sarj-blended-it",
  "version": "0.1.0",
  "description": "Premium mobile-barber booking platform",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/admin",
    "build": "npm run build --workspace=apps/admin",
    "start": "npm run start --workspace=apps/admin",
    "lint": "npm run lint --workspace=apps/admin && npm run lint --workspace=apps/customer"
  }
}
